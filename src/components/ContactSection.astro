---
import Container from './Container.astro';

interface Props {
    formTitle?: string;
    formSubtitle?: string;
}

const { formTitle, formSubtitle } = Astro.props;
---

<div class="bg-[#f3f4f6] py-16 lg:py-24">
    <Container>
        <div class="grid items-start gap-12 lg:grid-cols-2 lg:gap-16">
            <div>
                <slot>
                    <h2
                        class="font-title text-3xl leading-tight font-bold text-slate-900 lg:text-4xl"
                    >
                        ¿Necesitas un abogado penalista?
                    </h2>
                    <p class="mt-6 text-lg leading-relaxed text-slate-600">
                        Si tú o un ser querido enfrenta un proceso penal, no dudes
                        en contactarnos. Estamos aquí para escuchar su caso y
                        brindarle la mejor estrategia de defensa legal.
                    </p>

                    <div class="mt-12">
                        <p
                            class="text-sm font-bold tracking-wide text-slate-900 uppercase"
                        >
                            Consulta gratuita
                        </p>
                        <p class="font-title mt-2 text-3xl font-bold lg:text-4xl">
                            Llama al: +51 964 047 119
                        </p>
                    </div>
                </slot>
            </div>

            <div>
                {
                    formTitle && (
                        <div class="mb-8">
                            <h2 class="font-title text-3xl font-bold text-slate-900">
                                {formTitle}
                            </h2>
                            {formSubtitle && (
                                <p class="mt-2 text-lg font-medium text-slate-600">
                                    {formSubtitle}
                                </p>
                            )}
                        </div>
                    )
                }

                <form class="space-y-4">
                    <input
                        type="text"
                        placeholder="Tu Nombre"
                        class="w-full rounded border border-slate-300 bg-transparent px-4 py-3 focus:border-slate-500 focus:outline-none"
                    />
                    <input
                        type="email"
                        placeholder="Correo Electrónico"
                        class="w-full rounded border border-slate-300 bg-transparent px-4 py-3 focus:border-slate-500 focus:outline-none"
                    />
                    <input
                        type="tel"
                        placeholder="Número de Teléfono"
                        class="w-full rounded border border-slate-300 bg-transparent px-4 py-3 focus:border-slate-500 focus:outline-none"
                    />
                    <textarea
                        rows="4"
                        placeholder="Describe tu Caso"
                        class="w-full rounded border border-slate-300 bg-transparent px-4 py-3 focus:border-slate-500 focus:outline-none"
                    ></textarea>

                    <button
                        type="submit"
                        class="bg-[var(--color-theme)] w-full px-8 py-4 font-bold tracking-wider text-white uppercase transition-colors hover:bg-red-800"
                    >
                        Solicitar asesoría ahora
                    </button>
                </form>
            </div>
        </div>
    </Container>
</div>
